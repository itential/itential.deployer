# Itential Platform Installation Validation Report

- **Generated:** {{ ansible_date_time.iso8601 | default('Unknown') }}
- **Hostname:** {{ inventory_hostname | default('Unknown') }}
- **IP Address:** {{ ansible_default_ipv4.address | default('N/A') }}
- **OS:** {{ ansible_distribution | default('Unknown') }} {{ ansible_distribution_version | default('') }}

---

## Host Details

{% if platform_host_details is defined %}
### Operating System
- **Distribution:** {{ platform_host_details.os.distribution | default('Unknown') }} {{ platform_host_details.os.distribution_version | default('') }}
- **OS Family:** {{ platform_host_details.os.os_family | default('Unknown') }}
- **Kernel:** {{ platform_host_details.os.kernel | default('Unknown') }}
- **Architecture:** {{ platform_host_details.os.architecture | default('Unknown') }}
- **Hostname:** {{ platform_host_details.os.hostname | default('Unknown') }}
- **FQDN:** {{ platform_host_details.os.fqdn | default('Unknown') }}

### Hardware
- **CPU Count:** {{ platform_host_details.hardware.cpu.processor_count | default('Unknown') }}
- **CPU Cores:** {{ platform_host_details.hardware.cpu.processor_cores | default('Unknown') }}
- **CPU vCPUs:** {{ platform_host_details.hardware.cpu.processor_vcpus | default('Unknown') }}
- **Threads per Core:** {{ platform_host_details.hardware.cpu.processor_threads_per_core | default('Unknown') }}
- **Total Memory:** {{ platform_host_details.hardware.memory.memtotal_mb | default('Unknown') }} MB
- **Free Memory:** {{ platform_host_details.hardware.memory.memfree_mb | default('Unknown') }} MB
- **Swap Total:** {{ platform_host_details.hardware.memory.swaptotal_mb | default('Unknown') }} MB

### Disk Mounts
{% if platform_host_details.hardware.disk is defined and platform_host_details.hardware.disk | length > 0 %}
{% for disk in platform_host_details.hardware.disk %}
- **{{ disk.mount }}:** {{ disk.size_gb }} GB
{% endfor %}
{% else %}
- No disk information available
{% endif %}

### Networking
- **Primary IP:** {{ platform_host_details.networking.default_ipv4.address | default('N/A') }}
- **Gateway:** {{ platform_host_details.networking.default_ipv4.gateway | default('N/A') }}
- **Interface:** {{ platform_host_details.networking.default_ipv4.interface | default('N/A') }}
- **MAC Address:** {{ platform_host_details.networking.default_ipv4.macaddress | default('N/A') }}

### Security
{% if platform_host_details.security.selinux is defined %}
- **SELinux Status:** {{ platform_host_details.security.selinux.status | default('Unknown') }}
- **SELinux Mode:** {{ platform_host_details.security.selinux.mode | default('Unknown') }}
- **SELinux Type:** {{ platform_host_details.security.selinux.type | default('Unknown') }}
{% endif %}
{% if platform_host_details.security.firewalld is defined %}
- **Firewalld:** {{ platform_host_details.security.firewalld.state | default('Unknown') }}
{% endif %}
{% else %}
Host details not available
{% endif %}

---

## Service Status

{% if platform_service_status is defined and platform_service_status.status is defined %}
- **Service Name:** {{ platform_service_status.name | default('Unknown') }}
- **Service State:** {{ platform_service_status.status.ActiveState | default('Unknown') }}
- **Service SubState:** {{ platform_service_status.status.SubState | default('Unknown') }}
- **Service Enabled:** {{ platform_service_status.status.UnitFileState | default('Unknown') }}
{% else %}
- **Service Status:** Could not determine (service may not exist)
{% endif %}

**Process Running:** {{ 'YES ✓' if (platform_process is defined and platform_process.rc == 0) else 'NO ✗' }}

{% if platform_process is defined and platform_process.rc == 0 %}
**Process Details:**
```
{{ platform_process.stdout | default('N/A') }}
```
{% endif %}

---

## Configuration Files
- **Config File Exists:** {{ 'YES ✓' if (platform_conf_file is defined and platform_conf_file.stat.exists) else 'NO ✗' }}
{% if platform_conf_file is defined and platform_conf_file.stat.exists %}
- **Config File Path:** `/etc/itential/platform.properties`
- **Permissions:** {{ platform_conf_permissions.stdout | default('Unknown') if platform_conf_permissions is defined else 'Unknown' }}
{% endif %}

- **Systemd Unit File:** {{ 'YES ✓' if (platform_systemd_file is defined and platform_systemd_file.stat.exists) else 'NO ✗' }}
{% if platform_systemd_file is defined and platform_systemd_file.stat.exists %}
- **Unit File Path:** `/etc/systemd/system/itential-platform.service`
{% endif %}

---

## Critical Configuration Properties

- **server_id:** {{ platform_server_id.stdout | default('Unknown') }}
- **mongo_auth_enabled:** {{ platform_mongo_auth_enabled.stdout | default('Unknown') }}
- **mongo_user:** {{ (platform_mongo_user.stdout | default('')) or 'Default value' }}
- **mongo_auth_db:** {{ (platform_mongo_auth_db.stdout | default('')) or 'Default value' }}
- **mongo_db_name:** {{ platform_mongo_db_name.stdout | default('Unknown') }}
- **mongo_url:** {{ platform_mongo_url_masked | default('Unknown') }}
- **mongo_tls_enabled:** {{ platform_mongo_tls_enabled.stdout | default('Unknown') }}
- **redis_db:** {{ (platform_mongo_auth_db.stdout | default('')) or 'Default value' }}
- **redis_user:** {{ (platform_mongo_auth_db.stdout | default('')) or 'Default value' }}
- **redis_host:** {{ (platform_redis_host.stdout | default('')) or 'Unknown' }}
- **redis_sentinel_username:** {{ platform_redis_sentinel_username.stdout | default('Unknown') }}
- **redis_sentinels:** {{ platform_redis_sentinels.stdout | default('Unknown') }}
- **redis_name:** {{ platform_redis_name.stdout | default('Unknown') }}
- **redis_tls:** {{ (platform_redis_tls.stdout | default('')) or 'Unknown' }}
- **task_worker_enabled:** {{ platform_task_worker_enabled.stdout | default('Unknown') }}
- **job_worker_enabled:** {{ platform_job_worker_enabled.stdout | default('Unknown') }}
- **default_user_username:** {{ platform_default_user_username.stdout | default('Unknown') }}
- **webserver_http_enabled:** {{ platform_webserver_http_enabled.stdout | default('Unknown') }}
- **webserver_https_enabled:** {{ platform_webserver_https_enabled.stdout | default('Unknown') }}
- **webserver_http_port:** {{ platform_webserver_http_port.stdout | default('Unknown') }}
- **webserver_https_port:** {{ platform_webserver_https_port.stdout | default('Unknown') }}
- **webserver_https_key:** {{ platform_webserver_https_key.stdout | default('Unknown') }}
- **webserver_https_cert:** {{ platform_webserver_https_cert.stdout | default('Unknown') }}
- **log_file:** {{ platform_log_directory.stdout + "/" + platform_log_file.stdout | default('Unknown') }}
- **log_level:** {{ platform_log_level.stdout | default('Unknown') }}
- **webserver_log_file:** {{ platform_webserver_log_directory.stdout + "/" + platform_webserver_log_filename.stdout | default('Unknown') }}

---

## Custom Services

- **Custom Service Directory:** {{ platform_service_directory.stdout | default('Unknown') }}

## TLS Certificates

### HTTPS Webserver

#### Files

| File | Exists | Permissions | Path |
|------|--------|-------------|------|
| Certificate | {{ 'YES' if platform_https_cert_stat.stat.exists | default(false) else 'NO' }} | {{ platform_https_cert_stat.stat.mode | default('N/A') }} | {{ platform_https_cert_dest }} |
| Private Key | {{ 'YES' if platform_https_key_stat.stat.exists | default(false) else 'NO' }} | {{ platform_https_key_stat.stat.mode | default('N/A') }} | {{ platform_https_key_dest }} |
| CA Bundle | {{ 'YES' if platform_https_ca_stat.stat.exists | default(false) else 'NO' }} | {{ platform_https_ca_stat.stat.mode | default('N/A') }} | {{ platform_https_ca_dest }} |

PKI Base Directory: `{{ platform_pki_base_dir }}` — Owner: {{ platform_pki_dir_stat.stat.pw_name | default('N/A') }}:{{ platform_pki_dir_stat.stat.gr_name | default('N/A') }}, Mode: {{ platform_pki_dir_stat.stat.mode | default('N/A') }}

#### Certificate Details

- **Subject:** {{ platform_https_cert_subject.stdout | default('N/A') }}
- **Issuer:** {{ platform_https_cert_issuer.stdout | default('N/A') }}
- **Serial:** {{ platform_https_cert_serial.stdout | default('N/A') }}
- **Signature Algorithm:** {{ platform_https_cert_sig_alg.stdout | default('N/A') }}
- **Public Key Info:** {{ platform_https_cert_key_info.stdout_lines | default([]) | join(' | ') | default('N/A') }}
- **Extended Key Usage:** {{ platform_https_cert_eku.stdout | default('N/A') }}

#### Validity

{% if platform_https_cert_dates is defined and platform_https_cert_dates.rc is defined and platform_https_cert_dates.rc == 0 %}
- **Not Before:** {{ platform_https_cert_dates.stdout | regex_search('notBefore=(.+)', '\\1') | first | default('Unknown') }}
- **Not After:** {{ platform_https_cert_dates.stdout | regex_search('notAfter=(.+)', '\\1') | first | default('Unknown') }}
- **Expiry Warning:** {{ 'EXPIRING WITHIN 30 DAYS' if (platform_https_cert_expiry_warn.rc is defined and platform_https_cert_expiry_warn.rc == 1) else 'OK' }}
{% else %}
- Validity dates not available (certificate file missing or unreadable)
{% endif %}

{% if platform_https_ca_stat.stat.exists | default(false) %}
#### CA Bundle Validity

{% if platform_https_ca_dates is defined and platform_https_ca_dates.rc is defined and platform_https_ca_dates.rc == 0 %}
- **Not Before:** {{ platform_https_ca_dates.stdout | regex_search('notBefore=(.+)', '\\1') | first | default('Unknown') }}
- **Not After:** {{ platform_https_ca_dates.stdout | regex_search('notAfter=(.+)', '\\1') | first | default('Unknown') }}
- **Expiry Warning:** {{ 'EXPIRING WITHIN 30 DAYS' if (platform_https_ca_expiry_warn.rc is defined and platform_https_ca_expiry_warn.rc == 1) else 'OK' }}
{% else %}
- CA validity dates not available (CA file missing or unreadable)
{% endif %}
{% endif %}

#### Subject Alternative Names

```
{{ platform_https_cert_sans.stdout | default('Not available') }}
```

- **inventory_hostname ({{ inventory_hostname }}) in SANs:** {{ 'YES' if platform_https_hostname_in_san | default(false) else 'NO' }}
{% if ansible_host is defined and ansible_host != inventory_hostname %}
- **ansible_host ({{ ansible_host }}) in SANs:** {{ 'YES' if platform_https_ip_in_san | default(false) else 'NO' }}
{% endif %}

#### Validation

- **Cert-Key Match:** {{ 'YES' if platform_https_cert_key_match | default(false) else ('NOT VERIFIED' if platform_https_cert_key_match is not defined else 'NO') }}
- **Chain Valid:** {% if platform_https_cert_chain_valid is defined and platform_https_cert_chain_valid.rc is defined %}{{ 'YES — ' ~ platform_https_cert_chain_valid.stdout if platform_https_cert_chain_valid.rc == 0 else 'NO — ' ~ platform_https_cert_chain_valid.stdout }}{% else %}NOT CHECKED{% endif %}

#### Live TLS Handshake

{% if platform_https_tls_handshake is defined and platform_https_tls_handshake.stdout is defined and platform_https_tls_handshake.stdout != '' %}
```
{{ platform_https_tls_handshake.stdout }}
```
{% else %}
Not tested (HTTPS disabled, port unavailable, or CA missing)
{% endif %}

---

### MongoDB Client TLS

{% if platform_mongo_tls_enabled.stdout | default('') | bool %}
#### CA File

| File | Exists | Permissions | Path |
|------|--------|-------------|------|
| CA Bundle | {{ 'YES' if platform_mongodb_ca_stat.stat.exists | default(false) else 'NO' }} | {{ platform_mongodb_ca_stat.stat.mode | default('N/A') }} | {{ platform_mongodb_ca_dest }} |

{% if platform_mongodb_ca_stat is defined and platform_mongodb_ca_stat.stat.exists | default(false) %}
#### CA Details

- **Subject:** {{ platform_mongodb_ca_subject.stdout | default('N/A') }}
- **Issuer:** {{ platform_mongodb_ca_issuer.stdout | default('N/A') }}

{% if platform_mongodb_ca_dates is defined and platform_mongodb_ca_dates.rc is defined and platform_mongodb_ca_dates.rc == 0 %}
- **Not Before:** {{ platform_mongodb_ca_dates.stdout | regex_search('notBefore=(.+)', '\\1') | first | default('Unknown') }}
- **Not After:** {{ platform_mongodb_ca_dates.stdout | regex_search('notAfter=(.+)', '\\1') | first | default('Unknown') }}
- **Expiry Warning:** {{ 'EXPIRING WITHIN 30 DAYS' if (platform_mongodb_ca_expiry_warn.rc is defined and platform_mongodb_ca_expiry_warn.rc == 1) else 'OK' }}
{% else %}
- Validity dates not available
{% endif %}
{% endif %}

#### Live MongoDB TLS Handshake

Tested against: `{{ platform_mongo_tls_check_host | default('N/A') }}:27017`

{% if platform_mongodb_tls_handshake is defined and platform_mongodb_tls_handshake.stdout is defined and platform_mongodb_tls_handshake.stdout != '' %}
```
{{ platform_mongodb_tls_handshake.stdout }}
```
{% else %}
Not tested (CA missing or MongoDB host unavailable)
{% endif %}

{% else %}
MongoDB TLS is disabled (`platform_mongo_tls_enabled: false`).
{% endif %}

## Log Files

{% if platform_log_file is defined and platform_log_file_stat.stat.exists %}
- **Log files exist:** YES ✓
- **Log files permissions:** {{ platform_log_file_stat.stat.mode }}
{% else %}
- **Log files exist:** NO ✗
{% endif %}
{% if platform_web_log_file is defined and platform_web_log_file_stat.stat.exists %}
- **Web log files exist:** YES ✓
- **Web log files permissions:** {{ platform_web_log_file_stat.stat.mode }}
{% else %}
- **Web log files exist:** NO ✗
{% endif %}

---

## NodeJs
- **Node Version:** {{ platform_nodejs_version.stdout | default('Unknown') }}
- **Node Executable:** {{ platform_nodejs_path.stdout | default('Unknown') }}

---

## Python
- **Python Version:** {{ platform_python_version.stdout | default('Unknown') }}
- **Python Executable:** {{ platform_python_path.stdout | default('Unknown') }}
- **Pip Version:** {{ platform_pip_version.stdout | default('Unknown') }}
- **Python Modules:**
```
{{ platform_python_modules.stdout | default('Unknown') }}
```

---

## Connectivity

- **HTTP health check:** {{ 'YES ✓' if (platform_http_health_check.status is defined and platform_http_health_check.status == 200) else 'NO ✗' }}
- **HTTPS health check:** {{ 'YES ✓' if (platform_https_health_check.status is defined and platform_https_health_check.status == 200) else 'NO ✗' }}
- **MongoDB connectivity:** {{ 'YES ✓' if platform_mongodb_connection else 'NO ✗' }}
- **Redis connectivity:** {{ 'YES ✓' if platform_redis_connection else 'NO ✗' }}


