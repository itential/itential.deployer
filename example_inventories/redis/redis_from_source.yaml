---
all:
  vars:
    # The Platform release must be defined.
    #  - valid values: 6, 6.0
    platform_release: 6

  children:
    redis_master:
      hosts:
        redis01.example.com:
      vars:
        # In this example, Redis is being installed from source (default).
        # The Redis install from source variable does not need to be defined.
        # redis_install_from_source: true
