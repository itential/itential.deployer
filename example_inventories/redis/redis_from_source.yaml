---
all:
  vars:
    platform_release: 6

  children:
    redis_master:
      hosts:
        redis01.example.com:
      vars:
        # The default is to install Redis from source
        #redis_install_from_source: true
